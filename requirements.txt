# Direct dependencies of the pipeline, pinned for reproducibility.
# Pinned versions matter here: the same file will build the Docker image and,
# from it, the Apptainer/Singularity container used on the cluster.

requests==2.34.2      # stage 1: STRING REST API calls
pyyaml==6.0.3         # stage 1: read config.yaml
pandas==3.0.5         # stages 2-3: tabular I/O
networkx==3.6.1       # stage 2: graph construction, centrality, communities
matplotlib==3.11.1    # stage 3: network figure
snakemake==9.27.0     # workflow manager: runs the three stages from the Snakefile
snakemake-executor-plugin-slurm==2.8.0   # lets `snakemake --profile profiles/slurm` submit to SLURM
